package mx.mfpp.beneficioapp.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Define el color 'Purple80', generalmente usado como color primario
 * o de acento en el esquema de colores del tema claro (Light Theme).
 */
val Purple80 = Color(0xFFD0BCFF)

/**
 * Define el color 'PurpleGrey80', generalmente usado como color secundario
 * o neutro en el esquema de colores del tema claro (Light Theme).
 */
val PurpleGrey80 = Color(0xFFCCC2DC)

/**
 * Define el color 'Pink80', generalmente usado como color terciario
 * o de acento en el esquema de colores del tema claro (Light Theme).
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * Define el color 'Purple40', generalmente usado como color primario
 * o de acento en el esquema de colores del tema oscuro (Dark Theme).
 */
val Purple40 = Color(0xFF6650a4)

/**
 * Define el color 'PurpleGrey40', generalmente usado como color secundario
 * o neutro en el esquema de colores del tema oscuro (Dark Theme).
 */
val PurpleGrey40 = Color(0xFF625b71)

/**
 * Define el color 'Pink40', generalmente usado como color terciario
 * o de acento en el esquema de colores del tema oscuro (Dark Theme).
 */
val Pink40 = Color(0xFF7D5260)